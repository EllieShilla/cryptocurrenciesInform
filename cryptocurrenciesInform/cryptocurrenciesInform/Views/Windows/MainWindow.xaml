<Window x:Class="cryptocurrenciesInform.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:cryptocurrenciesInform.ViewModels"
        xmlns:local="clr-namespace:cryptocurrenciesInform"
        mc:Ignorable="d"
        Title="Title" Height="450" Width="800">

    <Window.DataContext>
        <vm:MainWindowViewModel/>
    </Window.DataContext>

    <DockPanel>

        <Menu DockPanel.Dock="Top">
            <TextBox Foreground="LightGray" Width="100" Text="{Binding Search, UpdateSourceTrigger=PropertyChanged}">
                <TextBox.InputBindings>
                    <KeyBinding Command="{Binding Path=SearchCurrency}" Key="Enter" />
                </TextBox.InputBindings>
            </TextBox>
        </Menu>
        <Grid>
            <ContentControl Content="{Binding CurrentPageViewModel}"/>
        </Grid>
    </DockPanel>
</Window>
